name: home
description: home

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  charts_flutter: ^0.6.0
  dio: ^2.1.2
  flutter_spinkit: ^3.1.0
  cupertino_icons: ^0.1.2
  date_format: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - images/
    - images/1.png
